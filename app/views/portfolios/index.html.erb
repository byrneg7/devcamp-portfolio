<h1> Portfolio Items </h1>
<% @portfolios.each do |record| %>
    <p> <%= record.title%> </p>
    <p> <%= record.subtitle%> </p>
    <p> <%= record.body%> </p>
    <p> <%= image_tag(record.thumb_image) unless record.thumb_image.nil? %> </p>
<% end %>

